{
  "name": "capabilityhostprotocol",
  "private": true,
  "workspaces": ["packages/ts-types"],
  "scripts": {
    "typecheck": "npm run typecheck --workspace @capabilityhostprotocol/types",
    "build": "npm run build --workspace @capabilityhostprotocol/types"
  }
}
