{
  "_comment": "Per-provider webhook signing secrets for local testing. Generate signed payloads with sign.py using the SAME secret.",
  "secrets": {
    "github": "dev-webhook-secret",
    "stripe": "dev-webhook-secret",
    "slack": "dev-webhook-secret",
    "generic": "dev-webhook-secret"
  }
}
