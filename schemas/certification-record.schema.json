{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://capabilityhostprotocol.dev/schemas/certification-record.schema.json",
  "title": "CertificationRecord",
  "description": "Formal attestation that a capability meets a declared maturity level (§11.4).",
  "type": "object",
  "required": ["capability_id", "level", "granted_by", "certified_at"],
  "properties": {
    "capability_id": {
      "type": "string",
      "description": "The capability ID being certified."
    },
    "level": {
      "type": "integer",
      "minimum": 1,
      "maximum": 7,
      "description": "Maturity level certified (1–7)."
    },
    "granted_by": {
      "type": "string",
      "description": "Identity of the certifier (person, team, or system)."
    },
    "certified_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when the certification was recorded."
    },
    "notes": {
      "type": ["string", "null"],
      "description": "Optional free-text notes about the certification."
    }
  },
  "additionalProperties": false
}
